name: Tests
on:
  - pull_request
jobs:

  unit-test:
    name: Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout latest code
        uses: actions/checkout@v2

      - name: Fetch Java & Gradle
        uses: actions/setup-java@v2
        with:
          distribution: 'temurin'
          java-version: '11'
          cache: 'gradle'

      - name: Build Application
        env:
          PODCAST_INDEX_SECRET: ${{ secrets.PODCAST_INDEX_SECRET }}
          PODCAST_INDEX_KEY: ${{ secrets.PODCAST_INDEX_KEY }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          ENV: TEST
        run: ./gradlew test --no-daemon

      - name: Test Report
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: Unit Test Results [ALL Tests]
          path: build/test-results/test/*.xml
          reporter: java-junit

#      - name: Publish Unit Test Results
#        uses: EnricoMi/publish-unit-test-result-action@v1
#        if: always()
#        with:
#          check_name: Unit Test Results [Summary & Failed]
#          comment_mode: create new
#          files: build/test-results/**/*.xml

  integration-test:
    name: Integration Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout latest code
        uses: actions/checkout@v2

      - name: Build Application
        env:
          PODCAST_INDEX_SECRET: ${{ secrets.PODCAST_INDEX_SECRET }}
          PODCAST_INDEX_KEY: ${{ secrets.PODCAST_INDEX_KEY }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          ENV: TEST
        run: ./gradlew integrationTest --no-daemon

      - name: Test Report
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: Integration test results [ALL Tests]
          path: build/test-results/integrationTest/*.xml
          reporter: java-junit

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/reports/tests/integrationTest

#      - name: Publish Integration Test Results
#        uses: EnricoMi/publish-unit-test-result-action@v1
#        if: always()
#        with:
#          check_name: Integration Test Results [Summary & Failed]
#          comment_mode: create new
#          files: build/test-results/**/*.xml